import styled from "styled-components/native";

export const Container = styled.View`
    //isso é apenas um exemplo pra não ficar sem nada no código
    flex: 1;
    padding: 16px;
    background-color: white;
`;
